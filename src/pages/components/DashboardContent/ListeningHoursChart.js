
// import React, { useState } from 'react';
// import './ListeningHoursChart.css';

/*import React, { useState } from 'react';
import './ListeningHoursChart.css';


// function ListeningHoursChart({ dailyListeningHours }) {
//   const maxHours = 24;
//   const [clickedHour, setClickedHour] = useState(null);

//   const handleBarClick = (day, hours) => {
//     setClickedHour(`${day}: ${hours} hours`);
//     setTimeout(() => setClickedHour(null), 2000);
//   };

//   return (
//     <div className="listening-hours-chart-container">
//       <h3>Users' Listening Hours (Past Week)</h3>
//       {clickedHour && <div className="clicked-info">{clickedHour}</div>}
//       <div className="chart">
//         {Object.entries(dailyListeningHours).map(([day, hours]) => (
//           <div key={day} className="bar-container">
//             <div
//               className="bar"
//               style={{ height: `${(hours / maxHours) * 100}%` }}
//               onClick={() => handleBarClick(day, hours)}
//             >
//               <span className="hour-label">{hours}h</span>
//             </div>
//             <div className="day">{day}</div>
//           </div>
//         ))}
//       </div>
//     </div>
//   );
// }


// export default ListeningHoursChart;

export default ListeningHoursChart;
*/

